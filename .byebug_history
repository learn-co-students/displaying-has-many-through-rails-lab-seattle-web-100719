exit
@doctor.appointments.each {|appointment| I18n.l(@appointment.appointment_datetime, format: :long).split.insert(3, "at").join(" ")}
@doctor.appointments.each do |appointment|
@doctor
exit
I18n.l(@doctor.appointments.first.appointment_datetime, format: :long).split.insert(3, "at").join(" ")
I18n.l(@doctor.appointments.first.appointment_datetime, format: :long).split.insert(3, "at")
I18n.l(@doctor.appointments.first.appointment_datetime, format: :long).split
I18n.l(@doctor.appointments.first.appointment_datetime, format: :long)
@doctor.appointments.first
@doctor.appointments
